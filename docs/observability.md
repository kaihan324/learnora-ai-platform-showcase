# Observability

Learnora tracks application health, AI latency, provider health, queue state, job latency, vector readiness, and semantic-memory status.

Operational visibility includes:
- liveness endpoint
- readiness endpoint
- protected Prometheus metrics
- Grafana dashboards
- admin `/ops` view

Management endpoints are intended to remain private or securely tunnelled rather than exposed directly to the Internet.